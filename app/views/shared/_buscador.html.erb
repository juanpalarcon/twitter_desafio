<form class="form-inline">

  <%= search_form_for @q do |f| %>
    <div class="">
      <%= f.label :content_cont, 'Twitter'  %>
      <%= f.search_field :content_cont %>
      <%= f.submit "Search" %>
    </div>
  <% end %>
    
